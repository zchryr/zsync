version: '3.8'

services:
  zsync:
    image: 'zacharyr/zsync:latest'
    container_name: 'zsync'
    volumes:
      - ./keys/id_rsa:/home/zsync/.ssh/id_rsa
      - ./args.json:/home/zsync/args.json
      - ./test:/home/zsync/local